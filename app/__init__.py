"""ProjectOne application package.

Version is declared here rather than read from package metadata, because the
project is deliberately not installed as a distributable package
([tool.uv] package = false). Keep this in step with pyproject.toml.
"""

__version__ = "0.1.0"
